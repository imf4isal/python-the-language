#known as regexes

# there is a library "re" in python. 
#regular expression is kind of pattern, by those, we can define pattern, match against this pattern, or extract by this pattern.


#re.search(pattern, string, flags=0)
#pattern - what we wanna search for,
#string - where we wanna search
#we can use whole bunch of flags, it's something that in general modify the behavior of function
